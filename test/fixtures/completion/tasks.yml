- name: Test playbook
  gather_facts: False
  hosts: localhost
  tasks:
    - name: Builtin module
      ansible.builtin.debug:
        msg: "Hello World"

    - vyos.vyos.vyos_prefix_lists:
        config:
          - afi:
            prefix_lists:

    - an
