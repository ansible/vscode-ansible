{
  "name": "@ansible/common",
  "version": "0.0.1",
  "description": "Browser-safe Ansible types, prompts, utilities, and parsers",
  "main": "./out/index.js",
  "types": "./out/index.d.ts",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "files": [
    "out/"
  ],
  "scripts": {
    "compile": "tsc -b",
    "watch": "tsc -b -w",
    "clean": "node -e \"require('fs').rmSync('out',{recursive:true,force:true})\""
  },
  "dependencies": {}
}
