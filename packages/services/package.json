{
  "name": "@ansible/developer-services",
  "version": "0.0.1",
  "description": "Ansible developer services - command execution, collection discovery, caching, and environment management",
  "main": "./out/index.js",
  "types": "./out/index.d.ts",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "files": [
    "out/",
    "src/data/"
  ],
  "scripts": {
    "compile": "tsc -b",
    "watch": "tsc -b -w",
    "clean": "node -e \"require('fs').rmSync('out',{recursive:true,force:true})\""
  },
  "dependencies": {
    "@ansible/common": "workspace:*",
    "js-yaml": "^4.1.1"
  }
}
